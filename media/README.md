Drop the promo cover images here:

- `step1_cover.jpg` — shown after the "Мехоҳам Дар Эфир Иштирок Кунам" button (step 1)
- `step2_cover.jpg` — shown at the start of step 2 (balance top-up)

Filenames/paths are configurable via `STEP1_COVER_IMAGE` / `STEP2_COVER_IMAGE` in `.env`.
Until a file is present, the bot falls back to sending the buttons without a photo.
